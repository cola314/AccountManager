<ui:ContentDialog x:Class="AccountManager.View.AccountView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AccountManager.View"
        mc:Ignorable="d"
        Title="Account" Height="500" Width="400"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        PrimaryButtonText="OK"
        DefaultButton="Primary"
        SecondaryButtonText="Cancel">
    <ui:SimpleStackPanel Spacing="12">
        <TextBox ui:ControlHelper.Header="Site" Text="{Binding Site}"/>
        <TextBox ui:ControlHelper.Header="ID" Text="{Binding Id}"/>
        <TextBox ui:ControlHelper.Header="Password" Text="{Binding Password}"/>
        <TextBox ui:ControlHelper.Header="Description" Text="{Binding Description}"
                 MaxHeight="150" TextWrapping="Wrap" AcceptsReturn="True"/>
    </ui:SimpleStackPanel>
</ui:ContentDialog>
